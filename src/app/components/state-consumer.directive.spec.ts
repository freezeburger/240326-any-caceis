import { StateConsumerDirective } from './state-consumer.directive';

describe('StateConsumerDirective', () => {
  it('should create an instance', () => {
    const directive = new StateConsumerDirective();
    expect(directive).toBeTruthy();
  });
});
